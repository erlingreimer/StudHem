interface PropertyFormDialogProps {
  open: boolean;
  onClose: () => void;
  mode: 'create' | 'edit';
  propertyId?: string;
}

export function PropertyFormDialog(_props: PropertyFormDialogProps) {
  return null;
}
